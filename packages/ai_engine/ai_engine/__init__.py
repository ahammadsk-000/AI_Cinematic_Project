"""Cineforge inference engine — pure, web-free generative pipeline library.

Importable standalone (e.g. inside a Colab cell). See ai_engine.interfaces for
the backend contracts and ai_engine.orchestrator.pipeline for the stage model.
"""
__version__ = "0.1.0"
